apiVersion: v1
kind: Service
metadata:
  name: my-preparation-app-service
spec:
  selector:
    app: my-preparation-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32000
  type: NodePort # Otwiera port węzła Minikube
